#version 330 core

in vec4 FragPos;

out vec4 FragColor;

//uniform float shadowScale;

void main() {
  //gl_FragDepth = gl_FragCoord.z;
  //FragColor = vec4(vec3(gl_FragCoord.z), 1);
  //FragColor = vec4(vec3(FragPos.z/shadowScale), 1);
  //gl_FragDepth = FragPos.z/shadowScale;
  //gl_FragDepth = gl_FragCoord.z/shadowScale;
  //gl_FragDepth = FragPos.z;
}
